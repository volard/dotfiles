### Input configuration
#
# Read `man 5 sway-input` for more information about this section.

input "type:touchpad" {
  dwt enabled
  tap enabled
  natural_scroll enabled
  middle_emulation enabled
}

# Layout codes:
# https://gist.github.com/jatcwang/ae3b7019f219b8cdc6798329108c9aee
input "type:keyboard" {
	# xkb_capslock disabled
	xkb_layout us,ru,am
	xkb_variant workman,,eastern

  # I don't use that, but I need it so on lock screen I could switch locale and type password
  # localectl list-x11-keymap-options | grep grp:
	xkb_options grp:alt_space_toggle,grp:caps_toggle
}


bindsym --to-code {
   $mod+Ctrl+n input "type:keyboard" xkb_switch_layout 0
   $mod+Ctrl+o input "type:keyboard" xkb_switch_layout 1
   $mod+Ctrl+e input "type:keyboard" xkb_switch_layout 2
}

# Enable numlock by default
# input type:keyboard xkb_numlock enabled

# Set keyboard layout and variant based on current system settings
#exec_always {
#    'swaymsg input type:keyboard xkb_layout "$(localectl status | grep "X11 Layout" | sed -e "s/^.*X11 Layout://")"'
#    'swaymsg input type:keyboard xkb_variant "$(localectl status | grep "X11 Variant" | sed -e "s/^.*X11 Variant://")"'
#}

# # Assign the same binding to "pkill -RTMIN+1 waybar" to send signal to
# # the Waybar keyboard module (so the module shows the updated layout).
# # This example uses Alt + left Shift.
# bindsym Alt+Shift_L exec "pkill -RTMIN+1 waybar"
