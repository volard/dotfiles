---
- name: Install niri packages from pacman
  community.general.pacman:
    name: "{{ pacman_item }}"
    state: present
  loop:
    - niri
    - xwayland-satellite
    - fuzzel
    - satty
    - wl-copy
    - jq
    - slurp
  loop_control:
    loop_var: pacman_item
  become: true
