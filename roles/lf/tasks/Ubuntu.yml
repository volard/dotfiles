---
- name: Install lf on Linux
  ansible.builtin.apt:
    name: "{{ apt_item }}"
    state: present
  loop:
    - lf
  become: true
  loop_control:
    loop_var: apt_item
  when: ansible_facts['os_family'] in ["Debian", "Ubuntu"]
