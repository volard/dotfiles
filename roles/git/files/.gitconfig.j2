# Thats GLOBAL (per user on machine) level config that should be placed 
# in C:\Users\\.gitconfig on Windows or 
# in ~ /.gitconfig in Unix 
# i.e. user's home directory 

# SYSTEM level placed 
# in /etc/gitconfig ni Unix
# and IDK about windows

# LOCAL level configuration in .git/config of a current project 

# https://stackoverflow.com/questions/1838873/visualizing-branch-topology-in-git/34467298#34467298



[user]
	name = {{ username }}
	email = {{ email_address }}

[credential "helperselector"]
	selected = manager

[init]
	defaultBranch = main

[core]
  # use unix line endings
  autocrlf = false
  safecrlf = warn

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[gui]
	encoding = UTF-8

[color]
    ui = always # may cause problems be careful

[alias]
    lg = lg1
    lg1 = lg1-specific --all
    lg2 = lg2-specific --all
    lg3 = lg3-specific --all

    lg1-specific = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)'
    lg2-specific = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(auto)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)'
    lg3-specific = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset) %C(bold cyan)(committed: %cD)%C(reset) %C(auto)%d%C(reset)%n''          %C(white)%s%C(reset)%n''          %C(dim white)- %an <%ae> %C(reset) %C(dim white)(committer: %cn <%ce>)%C(reset)'


