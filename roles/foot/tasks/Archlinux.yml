---
- name: Create foot symlink
  ansible.builtin.include_tasks: roles/common_tasks/tasks/create_symlinks.yml
  vars:
    symlink:
      src: "{{ role_path }}/files"
      dest: "{{ ansible_facts['user_dir'] }}/.config/foot"
      remove_existing: true
