but = but st -f (file) -v (verbose) = but stf
but reword [code] - rename branch
but stage [codes]
but commit -o (only staged)

stack branches

-a (add to another branch on top kinda)
but pr
but teardown - go back to git system
